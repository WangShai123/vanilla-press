# vanilla-press

A static documentation generator built on `markdown-it`.

## Usage

```bash
npm install -D vanilla-press
```

```json
{
  "scripts": {
    "dev": "vanilla-press dev",
    "build": "vanilla-press build"
  }
}
```

`vanilla-press` reads source files from `docs/` and writes the generated site to `dist/`.
