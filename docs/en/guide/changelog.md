# Changelog

## 1.2.10

- Added inline script support with `vp-script` blocks in Markdown for page-only scripts.
- Added footer script support with `docs/footerScript.ts` for site-wide bottom-of-body scripts.
- Updated dependencies such as `vanilla-jui` and `vanilla-signal`, and adapted the project to their newer APIs.
